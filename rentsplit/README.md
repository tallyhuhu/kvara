# Kvara workspace

The complete product, architecture, security, Base attribution, setup, and deployment documentation lives in the [repository README](../README.md).

```bash
npm install
npm run dev
```

Frontend: `http://localhost:3000`
Backend: `http://localhost:3001`
